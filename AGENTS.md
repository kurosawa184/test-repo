# AGENTS.md

このファイルは、このディレクトリ配下で作業するエージェント向けの共通ルールです。  
迷った場合は「安全・最小変更・再現性」を優先してください。

## 1) 基本方針
- 目的達成に必要な最小限の変更を行う。
- 既存の設計・命名・フォーマットに合わせる。
- 指示が競合する場合は、優先順位を次の通りにする：
  1. システム指示
  2. 開発者指示
  3. ユーザー指示
  4. この `AGENTS.md`
- 破壊的変更（大規模削除、互換性のない変更）は、明示的な依頼がない限り実施しない。

## 2) 作業手順（標準フロー）
1. 要件を短く要約し、実施方針を決める。
2. 関連ファイルのみ確認し、影響範囲を最小化する。
3. 変更を実装する（小さく、説明可能な単位）。
4. 可能な範囲でテスト/検証を行う。
5. 変更内容・検証結果・未実施事項（あれば理由）を報告する。

## 3) コーディング規約（汎用）
- 不要な抽象化を避け、可読性を優先する。
- コメントは「なぜ」を補足するために使い、「何をしているか」はコードで表現する。
- 既存のリンター/フォーマッター設定がある場合は従う。
- 無関係なリファクタリングは行わない。

## 4) テストと検証
- 変更箇所に最も近い粒度で検証する（ユニットテスト、既存テスト、最小実行確認など）。
- テスト環境の制約で実行できない場合は、理由と代替確認方法を明記する。
- 失敗したテストを放置して完了扱いにしない。

## 5) セキュリティと安全性
- シークレット（APIキー、パスワード、秘密情報）を生成・表示・コミットしない。
- 外部ネットワークや破壊的操作が必要な場合は、必要性を説明できる形で最小限に行う。
- 個人情報や機密情報をログや出力に含めない。

## 6) Git運用
- 1つの目的に対して、1つの論理的なコミットを基本とする。
- コミットメッセージは簡潔に「何をしたか」を示す。
- 生成物や一時ファイルを不要にコミットしない。

## 7) レスポンス方針
- 非専門家にも読める平易な説明を優先する。
- 専門用語を使う場合は短い注釈を添える。
- 変更ファイルと検証コマンドを明示する。

## 8) スコープと上書き
- より深い階層に別の `AGENTS.md` がある場合、その指示を優先する。
- 本ファイルにない事項は、既存プロジェクトの慣習に従う。
